name: CI

on:
  push:
    branches: [main]

jobs:
  test-and-lint:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up dummy secrets for CI
      run: |
        if [ -f "incognita/values.py.example" ]; then
          cp incognita/values.py.example incognita/values.py
        elif [ -f "src/values.py.example" ]; then
          cp src/values.py.example src/values.py
        fi
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    
    - name: Install uv
      uses: astral-sh/setup-uv@v4
      with:
        version: "latest"
    
    - name: Install dependencies
      run: uv sync
    
    - name: Run tests with coverage
      run: uv run pytest
    
    - name: Check formatting with black
      run: uv run black . --check
    
    - name: Run ruff linting
      run: uv run ruff check .
    
    - name: Upload coverage to Codecov
      if: always()
      uses: codecov/codecov-action@v4
      with:
        fail_ci_if_error: false
  
  deploy:
    runs-on: ubuntu-latest
    needs: test-and-lint
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up dummy secrets for CI
      run: |
        if [ -f "incognita/values.py.example" ]; then
          cp incognita/values.py.example incognita/values.py
        elif [ -f "src/values.py.example" ]; then
          cp src/values.py.example src/values.py
        fi
    
    - name: Deploy to Raspberry Pi (placeholder)
      run: |
        echo "🚀 Deployment placeholder"
        echo "TODO: SSH into Raspberry Pi and run install script"
        echo "Required secrets:"
        echo "  - SSH_PRIVATE_KEY: SSH key for Pi access"
        echo "  - PI_HOST: Raspberry Pi hostname/IP"
        echo "  - PI_USER: SSH username"
        echo "  - PI_PROJECT_PATH: Path to project on Pi"
